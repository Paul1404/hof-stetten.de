---
interface Props {
  id: string;
  label?: string;
  dark?: boolean;
  class?: string;
}

const { id, label = "Quelle", dark = false, class: className = "" } = Astro.props;
---
<a class:list={["source-ref", dark && "source-ref-dark", className]} href={`/quellen#${id}`}>{label}</a>
